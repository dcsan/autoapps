import { describe, expect, test } from "@jest/globals";
import { prisma } from "../lib/prisma";
import { UserFac } from "../models/UserFac";

describe("xx", () => {
  test("yy", async () => {
    expect(true).toBe(true);
  });
});
